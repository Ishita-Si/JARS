# Copy this file to .env.local for local development.

# URL of the FastAPI backend (see ../backend). Defaults to this value in
# lib/api.ts even if unset, so this file is only needed if your backend
# runs somewhere else.
NEXT_PUBLIC_API_URL=http://localhost:8000
